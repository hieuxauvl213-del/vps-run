name: Windows VPS

on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Run exe
      run: |
        dir
        start AngelChipEmulator.exe

    - name: Keep alive
      run: ping -t localhost
